{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — desktop window, dialog plugin for the folder picker, and asset:// protocol for the preview player.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
